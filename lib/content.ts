/**
 * Product content resolver — type detection: product NAME wins over stale Nose Pin category/tags.
 */
export {
  type GeneratedContent,
  type ProductLike,
  DIVA_NAMES,
  preferredTitle,
  pickDivaName,
  includedPieces,
  templateContent,
  resolveProductContent,
} from "./content.base";
